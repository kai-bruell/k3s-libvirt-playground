{
  "packages": [
    "terraform",
    "go-task",
    "git",
    "qemu",
    "sshpass",
    "libvirt",
    "kubectl"
  ],
  "shell": {
    "init_hook": [
      "echo 'Infrastructure-as-Code Terraform K3S Environment started.'",
      "if [ -f kubeconfig ]; then export KUBECONFIG=$(pwd)/kubeconfig && echo 'KUBECONFIG set to $(pwd)/kubeconfig'; fi"
    ]
  }
}
